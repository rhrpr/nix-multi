# This repository's encrypted secrets and recipients are maintainer-specific.
# Forks should replace this import with a new secrets.nix based on
# secrets.nix.example before creating any encrypted files.
import ./profiles/rhrpr/secrets.nix
