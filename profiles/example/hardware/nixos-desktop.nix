{ ... }:
{
  # Generate this file on the target NixOS host:
  #   sudo nixos-generate-config --show-hardware-config
  #
  # Add only host-specific settings here (filesystems, boot hardware,
  # GPU drivers, Secure Boot, monitor settings, and kernel parameters).
}
