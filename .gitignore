result
result/
.DS_Store
*.qcow2

# SSH Keys - NEVER commit private keys
*.pem
*.key
*_rsa
*_ed25519
*_ecdsa
id_rsa
id_ed25519
id_ecdsa*
known_hosts
known_hosts.old

# Age private keys - NEVER commit
*.txt
key.txt
keys.txt

# Decrypted secrets - NEVER commit  
*.decrypted
*.dec
*.plain

# Temporary files
*.tmp
*.temp
*~
.DS_Store

# Backup files
*.backup
*.bak
*.orig

# Age files are SAFE to commit (they're encrypted)
# *.age files should be committed

# VM ISO files and downloads
vm-iso/
*.iso

# Agent Deck task worktrees
.worktrees/
